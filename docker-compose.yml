services:
  app:
    build: .
    env_file:
      - .env
    environment:
      DB_DSN: ${JOURNAL_DB_DSN}
    ports:
      - "4000:4000"
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
